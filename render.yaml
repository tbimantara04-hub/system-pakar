services:
  - type: web
    name: system-pakar-app
    runtime: docker 
    dockerfilePath: Dockerfile 
    envVars:
      # ... (semua envVars Anda yang lain biarkan saja) ...
      - key: APP_URL
        value: $RENDER_EXTERNAL_URL
        
    # PASTIKAN HANYA INI:
    preDeployCommand: "php artisan migrate --force"